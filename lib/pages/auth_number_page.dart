import 'package:flutter/material.dart';

class AuthNumberPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('번호'),
      ),
      body: Center(
        child: Text('번호 페이지입니다.'),
      ),
    );
  }
}
