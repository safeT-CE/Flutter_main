import 'package:flutter/material.dart';

class RentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('대여하기'),
      ),
      body: Center(
        child: Text('대여하기 페이지입니다.'),
      ),
    );
  }
}

