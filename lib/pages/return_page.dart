import 'package:flutter/material.dart';

class ReturnPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('반납하기'),
      ),
      body: Center(
        child: Text('반납하기 페이지입니다.'),
      ),
    );
  }
}
